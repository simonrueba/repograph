{
  "name": "ariadne-mcp",
  "version": "0.2.0",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "ariadne-mcp": "dist/index.js"
  },
  "files": ["dist"],
  "engines": {
    "bun": ">=1.0.0"
  },
  "scripts": {
    "start": "bun run src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun && cp ../core/src/scip/scip.proto dist/scip.proto",
    "test": "vitest run"
  },
  "devDependencies": {
    "ariadne-core": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.27.0",
    "zod": "^3.23.0",
    "bun-types": "latest"
  }
}
