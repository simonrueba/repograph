{
  "name": "repograph",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "test": "vitest",
    "build": "bun run --filter '*' build",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
