{
  "name": "ariadne",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "test": "vitest",
    "build": "bun run --filter '*' build",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "bun-types": "^1.3.9",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
